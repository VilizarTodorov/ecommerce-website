import React from "react";
import "./styles.scss";

const CityPostalCodeWrapper = ({ children }) => {
  return <div className="city-postal-code-wrapper">{children}</div>;
};

export default CityPostalCodeWrapper;
