import React from "react";
import GeneralButton from "../GeneralButton";

const LoadMore = ({ loadMore }) => {
  return <GeneralButton onClick={loadMore}>load more</GeneralButton>;
};

export default LoadMore;
